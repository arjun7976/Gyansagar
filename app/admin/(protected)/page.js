import { getDashboardStatistics } from "../../../lib/admin-data";
const cards = [["Total Students", "students", "Registered student accounts"], ["Total Tests", "tests", "Tests configured in the system"], ["Total Questions", "questions", "MCQ question bank size"], ["Total Attempts", "attempts", "Recorded test attempts"]];
export default async function AdminDashboardPage() { let statistics; let error = false; try { statistics = await getDashboardStatistics(); } catch (caughtError) { console.error("Dashboard render failed:", caughtError.message); statistics = { students: 0, tests: 0, questions: 0, attempts: 0 }; error = true; } return <div className="mx-auto max-w-7xl"><div><p className="text-sm font-semibold text-blue-700">ADMIN OVERVIEW</p><h1 className="mt-1 text-3xl font-bold tracking-tight text-slate-950">Dashboard</h1><p className="mt-2 text-slate-600">A quick view of your institute assessment system.</p></div>{error && <p className="mt-6 rounded-lg bg-amber-50 px-4 py-3 text-sm text-amber-800">Live statistics are temporarily unavailable. Please check the database connection.</p>}<section className="mt-7 grid gap-4 sm:grid-cols-2 xl:grid-cols-4">{cards.map(([title, key, description]) => <article key={key} className="rounded-xl border border-slate-200 bg-white p-5 shadow-sm"><p className="text-sm font-medium text-slate-600">{title}</p><p className="mt-3 text-3xl font-bold text-slate-950">{statistics[key]}</p><p className="mt-2 text-xs text-slate-500">{description}</p></article>)}</section><section className="mt-8 rounded-xl border border-slate-200 bg-white p-6 shadow-sm"><h2 className="text-lg font-semibold text-slate-950">Recent Activity</h2><div className="mt-5 rounded-lg border border-dashed border-slate-300 px-4 py-10 text-center text-sm text-slate-500">No recent activity</div></section></div>; }
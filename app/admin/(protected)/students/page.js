import { connectToDatabase } from "../../../../lib/mongodb";
import User from "../../../../models/User";
export default async function StudentsPage() { let students = []; let error = false; try { await connectToDatabase(); students = await User.find({ role: "student" }).select("name email isActive createdAt").sort({ createdAt: -1 }).limit(20).lean(); } catch { error = true; } return <div className="mx-auto max-w-7xl"><h1 className="text-3xl font-bold tracking-tight text-slate-950">Students</h1><p className="mt-2 text-slate-600">Registered student accounts will appear here.</p><section className="mt-7 overflow-hidden rounded-xl border border-slate-200 bg-white shadow-sm">{error ? <p className="p-6 text-sm text-amber-800">Student data is temporarily unavailable. Please check the database connection.</p> : students.length === 0 ? <p className="p-10 text-center text-sm text-slate-500">No students registered yet.</p> : <div className="overflow-x-auto"><table className="w-full text-left text-sm"><thead className="bg-slate-50 text-slate-600"><tr><th className="px-5 py-4">Name</th><th className="px-5 py-4">Email</th><th className="px-5 py-4">Status</th></tr></thead><tbody>{students.map((student) => <tr key={student._id.toString()} className="border-t border-slate-100"><td className="px-5 py-4 font-medium text-slate-900">{student.name}</td><td className="px-5 py-4 text-slate-600">{student.email}</td><td className="px-5 py-4"><span className={`rounded-full px-2.5 py-1 text-xs font-semibold ${student.isActive ? "bg-emerald-50 text-emerald-700" : "bg-slate-100 text-slate-600"}`}>{student.isActive ? "Active" : "Inactive"}</span></td></tr>)}</tbody></table></div>}</section></div>; }
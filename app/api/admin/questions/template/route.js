import * as XLSX from "xlsx";
export function GET() { const worksheet = XLSX.utils.json_to_sheet([{ testId: "your_test_id", questionText: "What is CPU?", optionA: "Central Processing Unit", optionB: "Computer Personal Unit", optionC: "Central Program Unit", optionD: "Control Processing Unit", correctAnswer: "A", explanation: "CPU stands for Central Processing Unit.", marks: 1, difficulty: "Easy", subject: "Computer" }]); const workbook = XLSX.utils.book_new(); XLSX.utils.book_append_sheet(workbook, worksheet, "Questions"); const data = XLSX.write(workbook, { type: "buffer", bookType: "xlsx" }); return new Response(data, { headers: { "Content-Type": "application/vnd.openxmlformats-officedocument.spreadsheetml.sheet", "Content-Disposition": "attachment; filename=gyansagar-question-template.xlsx" } }); }
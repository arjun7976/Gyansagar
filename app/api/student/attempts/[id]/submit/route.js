import{NextResponse}from"next/server";import TestAttempt from"../../../../../../models/TestAttempt";import Test from"../../../../../../models/Test";import{connectToDatabase}from"../../../../../../lib/mongodb";import{currentStudent}from"../../../../../../lib/student-auth";import{score}from"../../../../../../lib/attempts";
export async function POST(_,{params}){const s=await currentStudent();const{id}=await params;if(!s)return NextResponse.json({success:false,message:"Unauthorized"},{status:401});await connectToDatabase();const a=await TestAttempt.findById(id),t=a&&await Test.findById(a.testId);if(!a)return NextResponse.json({success:false,message:"Attempt not found"},{status:404});if(a.studentId.toString()!==s.id)return NextResponse.json({success:false,message:"Forbidden"},{status:403});if(a.status!=="in_progress")return NextResponse.json({success:false,message:"Attempt already submitted"},{status:400});await score(a,t);return NextResponse.json({success:true,data:{attemptId:a._id}})}
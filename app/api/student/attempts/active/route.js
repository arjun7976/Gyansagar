import{NextResponse}from"next/server";import TestAttempt from"../../../../../models/TestAttempt";import Test from"../../../../../models/Test";import{connectToDatabase}from"../../../../../lib/mongodb";import{currentStudent}from"../../../../../lib/student-auth";import{remaining}from"../../../../../lib/attempts";
export async function GET(){const s=await currentStudent();if(!s)return NextResponse.json({success:false,message:"Unauthorized"},{status:401});await connectToDatabase();const a=await TestAttempt.findOne({studentId:s.id,status:"in_progress"}).sort({startedAt:-1});if(!a)return NextResponse.json({success:true,data:null});const t=await Test.findById(a.testId).select("title duration");return NextResponse.json({success:true,data:{attemptId:a._id,testId:a.testId,title:t?.title,timeRemaining:remaining(a,t.duration)}})}
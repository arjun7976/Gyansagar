import{NextResponse}from"next/server";import Test from"../../../../models/Test";import Question from"../../../../models/Question";import TestAttempt from"../../../../models/TestAttempt";import{connectToDatabase}from"../../../../lib/mongodb";import{currentStudent}from"../../../../lib/student-auth";
export async function GET(){try{if(!await currentStudent())return NextResponse.json({success:false,message:"Unauthorized"},{status:401});await connectToDatabase();const tests=await Test.aggregate([{$match:{status:"published"}},{$lookup:{from:"questions",localField:"_id",foreignField:"testId",as:"q"}},{$addFields:{questionCount:{$size:"$q"}}},{$project:{q:0}}]);return NextResponse.json({success:true,data:tests})}catch{return NextResponse.json({success:false,message:"Unable to load tests"},{status:500})}}
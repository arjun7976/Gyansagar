"use client";
import { useRouter } from "next/navigation";
import { useState } from "react";
const defaults = { title: "", subject: "", description: "", instructions: "", duration: 30, totalMarks: 100, negativeMarks: 0, passingPercentage: 40, shuffleQuestions: true, shuffleOptions: false, showResultImmediately: true, maxAttempts: 1, startDate: "", endDate: "" };
export default function TestForm({ initialData }) { const router = useRouter(); const init = {...initialData}; if(init.startDate) init.startDate = new Date(init.startDate).toISOString().slice(0, 16); if(init.endDate) init.endDate = new Date(init.endDate).toISOString().slice(0, 16); const [form, setForm] = useState({ ...defaults, ...init }); const [error, setError] = useState(""), [saving, setSaving] = useState(false); const update = (key, value) => setForm({ ...form, [key]: value }); async function submit(e) { e.preventDefault(); setSaving(true); setError(""); const method = initialData?._id ? "PUT" : "POST"; const url = initialData?._id ? `/api/admin/tests/${initialData._id}` : "/api/admin/tests"; const payload = {...form}; if(!payload.startDate) payload.startDate = null; if(!payload.endDate) payload.endDate = null; const response = await fetch(url, { method, headers: { "Content-Type": "application/json" }, body: JSON.stringify(payload) }); const data = await response.json(); if (!data.success) { setError(data.message); setSaving(false); return; } router.push("/admin/tests"); router.refresh(); } return <form onSubmit={submit} className="mx-auto max-w-3xl space-y-5 rounded-xl border border-slate-200 bg-white p-6 shadow-sm"><h1 className="text-2xl font-bold">{initialData ? "Edit Test" : "Create Test"}</h1>{initialData?.status === "published" && <p className="rounded-lg bg-amber-50 p-3 text-sm text-amber-800">This test is published. Review changes carefully before saving.</p>}{[["title","Test Name"],["subject","Subject"]].map(([key,label]) => <label key={key} className="block text-sm font-medium">{label}<input required value={form[key]} onChange={(e) => update(key,e.target.value)} className="mt-1 w-full rounded-lg border border-slate-300 p-3" /></label>)}<label className="block text-sm font-medium">Description<textarea value={form.description} onChange={(e) => update("description",e.target.value)} className="mt-1 w-full rounded-lg border border-slate-300 p-3" /></label><div className="grid gap-4 sm:grid-cols-3">{[["duration","Duration (minutes)"],["totalMarks","Total Marks"],["negativeMarks","Negative Marks"],["passingPercentage","Passing Percentage"]].map(([key,label]) => <label key={key} className="text-sm font-medium">{label}<input type="number" min="0" value={form[key]} onChange={(e) => update(key,e.target.value)} className="mt-1 w-full rounded-lg border border-slate-300 p-3" /></label>)}</div><label className="block text-sm font-medium">Instructions<textarea value={form.instructions} onChange={(e) => update("instructions",e.target.value)} className="mt-1 w-full rounded-lg border border-slate-300 p-3" /></label><div className="grid gap-4 sm:grid-cols-3"><label className="text-sm font-medium">Max Attempts<input type="number" min="1" required value={form.maxAttempts} onChange={(e) => update("maxAttempts",e.target.value)} className="mt-1 w-full rounded-lg border border-slate-300 p-3" /></label><label className="text-sm font-medium">Start Date (Optional)<input type="datetime-local" value={form.startDate} onChange={(e) => update("startDate",e.target.value)} className="mt-1 w-full rounded-lg border border-slate-300 p-3" /></label><label className="text-sm font-medium">End Date (Optional)<input type="datetime-local" value={form.endDate} onChange={(e) => update("endDate",e.target.value)} className="mt-1 w-full rounded-lg border border-slate-300 p-3" /></label></div><div className="grid gap-3 sm:grid-cols-2">{[["shuffleQuestions","Shuffle Questions"],["shuffleOptions","Shuffle Options"],["showResultImmediately","Show Result Immediately"]].map(([key,label]) => <label key={key} className="flex gap-3 rounded-lg bg-slate-50 p-3 text-sm font-medium"><input type="checkbox" checked={form[key]} onChange={(e) => update(key,e.target.checked)} />{label}</label>)}</div>{error && <p className="rounded-lg bg-red-50 p-3 text-sm text-red-700">{error}</p>}<button disabled={saving} className="rounded-lg bg-blue-700 px-5 py-3 font-semibold text-white disabled:bg-blue-400">{saving ? "Saving..." : "Save Test"}</button></form>; }